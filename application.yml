eureka:
  client:
    service-url:
      defaultZone: http://hi:ruben@localhost:8080/eureka/,http://hi:ruben@localhost:8081/eureka/
spring:
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest
